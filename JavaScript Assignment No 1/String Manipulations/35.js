// Convert the First Letter of a String into UpperCase

let str = "hello"; 
let capitalizedStr = str.charAt(0).toUpperCase() + str.slice(1);

console.log("String with first letter capitalized: " + capitalizedStr);
