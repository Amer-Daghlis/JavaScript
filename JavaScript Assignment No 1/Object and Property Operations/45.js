// Count the Number of Keys/Properties in an Object

let person = { name: "Amer", age: 20, city: "Salfeet" };

let numberOfKeys = Object.keys(person).length;
console.log("Number of keys: " + numberOfKeys);
